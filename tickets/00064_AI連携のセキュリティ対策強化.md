# #00064 AI連携のセキュリティ対策強化

- 対象リポジトリ: backend
- ステータス: 完了
- type: fix
- 関連ドキュメント: [docs/システム要件定義書.md](../docs/システム要件定義書.md)（6章外部インターフェース要件・7.3セキュリティ要件） / [#00025 AI連携方針のブラッシュアップ](./00025_AI連携方針のブラッシュアップ.md) / [#00033 問題生成APIのAI・音声合成実連携化](./00033_問題生成APIのAI音声合成実連携化.md)

## 実現すること

`ielts-creater-backend`（`origin/develop`）のOpenAI連携実装（`OpenAiReadingQuestionGenerator`/`OpenAiListeningQuestionGenerator`等）をセキュリティ観点でレビューした結果、以下4点の対策不足が判明した。要件定義書・チケット全体を確認したが「プロンプトインジェクション」という語自体が一件も存在せず、検討漏れの領域だった。本チケットでこれらを解消する。

一方、APIキー管理（環境変数/Secrets Manager、ハードコードなし）、ログ出力方針（プロンプト全文・APIキーを出力しない設計）、外部API呼び出しのモック化テストは既に妥当な実装がされており、対象外とする。

1. **プロンプトインジェクション対策の欠如**: `OpenAiReadingQuestionGenerator.java:57-69`、`OpenAiListeningQuestionGenerator.java:60-73`でユーザー入力`topic`（`QuestionSetCreateRequest.java`、`@Size(max=100)`のみでサニタイズなし）を無防備にプロンプトへ直接埋め込んでいる。`response_format: json_schema strict:true`でJSON構造の破壊は防げるが、生成内容への注入（不適切な指示への追従等）は防げていない。system promptへの防御的な文言追加、およびuser prompt内での`topic`のデリミタ分離により緩和する。
2. **出力側のセキュリティ対策不足**: `reading-question-schema.json`/`listening-question-schema.json`の各stringプロパティに`maxLength`がなく、生成本文もHTML/スクリプトのサニタイズなしでDB保存・API応答に出る。JSON Schemaへの`maxLength`追加と、永続化前のサニタイズ処理を追加する。
3. **コスト濫用対策の不足**: OpenAIリクエストボディに`max_tokens`指定がなく、出力長に歯止めがない。レート制限もユーザー単位で1日2回のみ（IP/グローバル制限なし）。リクエストへの`max_tokens`設定を追加し、追加のレート制限の要否を検討する。
4. **エラーハンドリングの粗さ**: `QuestionSetGenerationWorker.java:73-77`でOpenAIの生エラーメッセージ（`e.getMessage()`）をそのまま`generation_error`カラムへ保存している。OpenAI由来の生文字列を含まない定型エラーメッセージへ変換して保存する。

## 受け入れ条件

- [x] `OpenAiReadingQuestionGenerator`/`OpenAiListeningQuestionGenerator`のsystem promptに、ユーザー入力`topic`内の指示文に従わない旨の防御的な文言を追加し、user prompt内で`topic`をデリミタで区切って埋め込む
- [x] `QuestionSetCreateRequest`の`topic`に対し、生成品質を損なわない範囲で制御文字・改行の除去/正規化を追加する
- [x] 生成された自由記述テキストの長さ上限チェックを追加する（実装時の調査で、OpenAI Structured Outputs〈`strict:true`〉はJSON Schemaの`maxLength`等の文字列制約キーワードをサポートしないことが判明したため、Schema変更ではなく`GenerationRuleValidator`でのアプリ側検証として実装。当初案の「JSON Schemaへの`maxLength`追加」から方針変更）
- [x] 生成本文（パッセージ本文・設問文・選択肢等）にHTMLタグらしき文字列が含まれていないかを`GenerationRuleValidator`で検証し、違反時は既存の1回リトライ→`status=FAILED`のフローに乗せる（フロントエンドが素のテキスト補間でReact自動エスケープに依存しているため、バックエンド側でのHTMLエンティティエスケープは短縮形等の表示を壊すため行わず、検出→再生成の方式とした）
- [x] OpenAIリクエストに`max_tokens`を設定する（`app.openai.max-tokens`、既定2000）
- [x] IPベース/グローバルなレート制限の要否を検討し、必要であれば追加する（不要と判断した場合はその理由を作業ログに残す） — 追加しないと判断（理由は作業ログ参照）
- [x] `QuestionSetGenerationWorker`でOpenAI由来の生エラーメッセージをDBへそのまま保存せず、定型エラーメッセージへ変換する
- [x] 上記変更を検証する単体テストを追加する（`OpenAiReadingQuestionGeneratorTest`/`OpenAiListeningQuestionGeneratorTest`等）
- [x] `docs/システム要件定義書.md`（7.3セキュリティ要件）にAI固有のセキュリティ対策方針（プロンプトインジェクション対策・出力検証）を追記する
- [x] backendリポジトリ`docs/API一覧.md`等の関連ドキュメントを実装と一致させる
- [x] `./gradlew spotlessCheck test integrationTest`がグリーンであることを確認する

## 作業ログ

- 2026-08-23 起票
- 2026-08-23 実装完了。[backendリポジトリ PR #41](https://github.com/h-fujiwara-dev/ielts-creater-backend/pull/41)をdevelopへマージ。受け入れ条件を全て満たしステータスを完了に更新
  - IPベース/グローバルなレート制限は追加しないと判断した。理由: (1) 通常ユーザーはCognito認証必須のため未認証の大量リクエストという参入障壁がある、(2) 認証不要のゲスト（共有デモアカウント）は既存の`GuestQuotaInterceptor`でIPアドレス単位の日次上限（既定3回）が設定済みであり、AI生成エンドポイントに対する濫用対策は既に存在する。infra層（API Gateway/WAF）にもスロットリングは存在しないが、アプリ層の対策で十分と判断した
  - 実装過程で、`QuestionSetGenerationService#checkDailyLimit`がcount→INSERTを別トランザクションで行っており、同時リクエストで1日2回の上限をすり抜けられるレースコンディションを発見した。コスト濫用対策の趣旨に合致するため、当初のスコープ外だったが本チケットで一緒に修正した（新規`QuestionSetCreator`でPostgresのアドバイザリロックにより1トランザクションでアトミックに実施。同時リクエストで上限を超えないことを検証する結合テストを追加）
