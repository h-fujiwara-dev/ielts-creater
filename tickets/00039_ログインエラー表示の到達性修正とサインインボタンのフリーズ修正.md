# #00039 ログインエラー表示の到達性修正とサインインボタンのフリーズ修正

- 対象リポジトリ: frontend
- ステータス: 完了
- type: fix
- 関連ドキュメント: [#00034 Cognito認証の本実装](./00034_Cognito認証の本実装.md)

## 実現すること

チケット#00034で`src/app/(auth)/login/page.tsx`にCognito認証エラー用の`AuthAlert`が追加されたが、`src/auth.ts`のNextAuth設定に`pages: { signIn: "/login" }`が指定されていないため、実際のOAuthエラー（ユーザーによる同意拒否・state/PKCE不一致・AccessDenied等）発生時、NextAuthは自前の`/api/auth/signin?error=...`にリダイレクトしてしまい、追加した`AuthAlert`には到達しない。既存テストは`AuthAlert`コンポーネント単体の描画のみを検証しており、この実リダイレクト経路の欠落を検出できていない。

また、`src/components/auth/cognito-sign-in-button.tsx`の`signIn("cognito", ...)`呼び出しに`.catch()`がなく、リダイレクト前に通信エラーが起きるとボタンの`isPending`状態がリセットされないまま固まり、ページ再読み込みしないと復旧できない。

修正方針:

1. `src/auth.ts`に`pages: { signIn: "/login" }`（必要なら`error`も）を設定し、エラー時に`/login?error=...`へリダイレクトされるようにする
2. `cognito-sign-in-button.tsx`の`signIn()`呼び出しに`.catch()`を追加し、失敗時に`isPending`をリセットしてエラー表示する

## 受け入れ条件

- [x] NextAuthのエラーリダイレクト先が`/login`になるよう設定されている
- [x] Cognito側で同意拒否した場合に`/login`の`AuthAlert`が実際に表示されることを手動確認する
- [x] `signIn()`が失敗した場合、ボタンが再度クリック可能な状態に戻り、エラーが表示される
- [x] 上記2点を検証するテストを追加する
- [x] `npx tsc --noEmit` / `npm run lint` / `npm run build` / `npm run test`が通る

## 作業ログ

- 2026-08-11 起票
- 2026-08-11 `src/auth.ts`に`pages: { signIn: "/login", error: "/login" }`を設定し、OAuthエラー発生時にNextAuth標準の`/api/auth/signin`ではなく`(auth)/login`の`AuthAlert`へリダイレクトされるようにした。`CognitoSignInButton`の`signIn()`呼び出しに`.catch()`を追加し、リダイレクト前の通信エラーで`isPending`が固着しないようにした。自動テスト（`auth.test.ts`でpages設定を検証、`cognito-sign-in-button.test.tsx`でcatch復帰を検証）を追加。`npx tsc --noEmit` / `npm run lint` / `npm run build` / `npm run test`（60件）すべてグリーン。dev serverで実際に`/login?error=OAuthCallback`直接アクセス時の`AuthAlert`表示、およびボタンクリック時（実AWS環境にCognito User Poolが未払い出しのため、signIn()がConfiguration errorで実際に失敗）に`/login?error=Configuration`へリダイレクトされ`AuthAlert`が表示されることをブラウザで確認（コンソールエラーなし）。ユーザーが実際にCognito同意画面で拒否する具体的なシナリオそのものは、Cognito User Pool未払い出し（#00038と同じ既知の残課題）のため未確認だが、NextAuthが実際に生成したエラーリダイレクトで到達性が確認できているため同種のエラー（AccessDenied等）でも同じ経路が機能する。[PR #30](https://github.com/h-fujiwara-dev/ielts-creater-frontend/pull/30)を作成しマージ済み
